# 运行脚本时落在工作目录的批注缓存（README：建议忽略）
.zotero-annotations/

# Python 字节码缓存
__pycache__/
*.pyc

# PyInstaller 打包产物（spec 与 build_exe.bat 保留，产物不提交）
build/
dist/
*.spec.bak

out/

# Zotero PDF Bridge：源码入仓，.xpi 仅作为 GitHub Release artifact 发布
zotero-pdf-bridge/*.xpi

# 不提交源码打包 zip / 临时 PDF / 测试输出
*.zip
*.pdf
